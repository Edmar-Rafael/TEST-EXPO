import { StyleSheet } from "react-native";

export const refreshStyles = StyleSheet.create({
  safa_area_view: {
    width: '70%',
    height: 250,
    zIndex: 9,
    position: 'absolute',
    top: 10,
  },

  scroll_view: {
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    height: '100%',
  }
})