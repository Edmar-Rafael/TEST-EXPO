import { StyleSheet } from "react-native";

export const createStyles = StyleSheet.create({
  tab: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    width: '100%',
    height: '5%',
  },

  create_movies: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    height: '95%',
    gap: 25
  }
})